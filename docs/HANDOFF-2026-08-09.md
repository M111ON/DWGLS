# DWGLS Handoff — 2026-08-09 (geo_jump session)

## สถานะปัจจุบัน

- **Commit:** `6d5573b` — geo_jump explorer + stride order sweep
- **Baseline:** `make test` = 25/25 PASS (TIER1 22 + TIER2 3) — ไม่ regression
- **Board:** `python tools/inbox_board.py list` — #23 เพิ่มแล้ว (pending 17, done 5)

## ⭐ ความรู้ใหม่ (session นี้)

### 1. geo_jump = MAZE WALLS ไม่ใช่ filling
```
Hilbert/Peano = structure (กำแพงอยู่กับที่) data เดินผ่าน
Hilbert/Peano ≠ space-filling walker — walk จาก slot 0 ไม่ไปไหน
geo_jump(node, type) = position-in-maze (bijective map ไม่ใช่ "next cell")
```

### 2. Sacred numbers — ยืนยันจาก code 🎯
| Constant | Value | Source file | หน้าที่ |
|----------|-------|-------------|--------|
| TRIT_MOD | 27 (3³) | `FGLS_new/collection/active_updates/frustum_trit.h` | trit dimension |
| L38_FED_SV | 34 | `FGLS_new/collection/core/pogls_engine/core/pogls38_fed_bridge.h` | step vector (FROZEN) |
| CYL_SIDE_FULL | 54 (6×9) | `FGLS_new/collection/core/core/geo_cylinder.h` | bridge |
| FRAME_STRIDE | 37 | `DWGLS/core/geo_frame_seek.h` | **1440-cycle ONLY** |
| GEO_FIBO_CLOCK | 1440 | geo_jump.h | cycle |
| GEO_FULL | 20736 | geo_jump.h | full space |

### 2. MOD order sweep — ค้นพบ stride 5
```
MOD walk: node' = (node × stride) % 20736

stride  5 → order 1728 = MAX (lcm(64,54)) = 1 pentagon (12³=1728)
stride 17 → 144
stride 37 → 576   (37 อยู่ในโดเมน 1440 ไม่ใช่ 20736)
stride 27 → 66    (gcd≠1)
stride 34 → 35    (gcd≠1)

12 orbits × 1728 = 20736 = FULL space
**MOD stride เดียว cover ได้ max 1728 = 1 pentagon**
```

### 3. 5-tetra compound chain
```
5 tetra × 12 origins = 60
60 × 12 sets = 720
720 × 2 invert = 1440 = GEO_FIBO_CLOCK ✓
20736 / 1440 = 14.4 → 0.4 threshold (geometry-given, ไม่ใช่ tuned)
```

### 4. Capo / Invert
- Capo key=10 → offset 1440 (clock cycle)
- Capo key=12 → offset 1728 (1 pentagon)
- INVERT = mirror 6-cycle sink (0→95→48→47→96→95→0)

## ไฟล์ใหม่ (uncommitted → แล้ว commit ไปแล้ว)

```
tests/geo_jump_explore.c      — explorer 9 tests
tests/geo_jump_real_test.c    — MOD sweep 27/34/17/54/162/16813
tests/mod_order_sweep.c       — brute-force stride order (stride 5 → 1728)
docs/geo-jump-explorer-2026-08-09.md — บทสรุปเต็ม
```

Build:
```bash
gcc -O2 -DGEO_JUMP_INLINE -I../../FGLS_new/collection/geo_jump_module/include \
    -o build/geo_jump_explore tests/geo_jump_explore.c -lm
```

## งานค้าง (next possible)

1. **KIS-timeline formula (#6)** — formalize as math formula (ยัง open)
2. **Batch convert GGUF→GEO (#3)** — blocked = geo_frame_seek.h ต้อง copy จาก FGLS_new → DWGLS
   - FGLS มี file: `I:/FGLS_new/collection/dgls/geo/include/geo_frame_seek.h`
3. **Use stride-5 (order 1728) เป็น MOD หลัก** สำหรับ 20736 — แทน 37 ที่ผิดโดเมน
4. Memory: fact 155 (geo_jump) — memory 79%, user profile 75%

## Key decisions (ต้องเอาไปต่อ)

- Hilbert/Peano = maze walls (data moves, structure still)
- 27/34/54 = sacred abundance numbers แต่ไม่ใช่ stride ใน 20736
- 37 อยู่ 1440-cycle เดียว — อย่าใช้กับ 20736
- 0.4 threshold = 20736/1440 remainder